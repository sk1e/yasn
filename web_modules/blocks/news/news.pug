mixin news(title, date, image, mix)
  .news(class=mix || '')
    img.news__image(src=require("blocks/news/images/" + image) alt=title + ' image')
    .news__description
      .news__title= title
      .news__date= date
      .news__text
        block
