mixin tick-box(type, value, state)
  .tick-box(class=`tick-box_state_${state}`)
    .fa.fa-check
    input.tick-box__input(type=type
      checked=(state === 'on')
      autocomplete='off'
      name=attributes.name
      value=value
      id=`${value}-field`)
