mixin tooltip(theme, pointerLocation)
  .tooltip
    .tooltip__body(class=`tooltip__body_theme_${theme}`)
      .tooltip__pointer(class=`tooltip__pointer_location_${pointerLocation}`)
      .tooltip__text
        block
    