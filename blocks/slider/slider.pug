mixin slider(type)
  case type
    when "tooltiped"
      .slider.slider_type_tooltiped
        .ui-slider-handle
          +tooltip("dark-2", "slider", "bottom")
    when "scaled"
      .slider.slider_type_scaled
        .slider__scale
          each n in [0, 25, 50, 75, 100]
            div=n
